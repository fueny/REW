# 应用配置
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5050

# 安全配置
# 生成一个安全的密钥: python -c "import secrets; print(secrets.token_hex(24))"
SECRET_KEY=your_secret_key_here

# 路径配置
# 如果需要自定义路径，取消下面的注释并设置值
# UPLOAD_FOLDER=/path/to/uploads
# DATABASE_PATH=/path/to/database.db

# 服务器配置
# 域名配置
DOMAIN_NAME=vocabulary.fueny.cn
